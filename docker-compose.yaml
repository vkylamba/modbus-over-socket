version: "3"
services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - 8024:8024
      - 9024:9024
      - 8080:8080

volumes:
  app-python-site-packages:
    driver: local
  app-usr-local-bin:
    driver: local
